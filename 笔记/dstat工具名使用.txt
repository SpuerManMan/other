1、执行命令dstat。显示为系统的整体的性能信息
2、执行命令dstat -c。显示系统整体的cpu情况。
显示各个信息为CPU用户占用，系统占用，空闲，等待，中断，软件中断等信息

3、执行命令dstat -C cpu的编号 可查看cpu0与1的状态信息
4、执行命令dstat -d 查看系统的磁盘的读写数据大小

5、执行命令dstat -n 查看系统的网络状态

6、执行命令dstat -l 查看系统的负载情况

7、执行命令dstat -m 查看系统的内存信息
8、执行命令dstat -p 查看系统的进程信息

9、执行命令dstat -r 查看系统的I/O请求情况